using DataLayer;

namespace BusinessLayer.Implementations
{
    public class ShoppingCartRepository : Repository
    {
        public ShoppingCartRepository(ApplicationContext context) : base(context)
        {
        }
    }
}
